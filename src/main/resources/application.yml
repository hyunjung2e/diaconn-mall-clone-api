spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/diaconn-mall-clone?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 또는 create, validate, none 등
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

# (선택) 서버 포트 변경
# server:
#   port: 8081
